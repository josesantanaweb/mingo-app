# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String
  refreshToken: String
  requiresTwoFactor: Boolean
  userId: String
}

type Bet {
  createdAt: DateTime!
  id: ID!
  market: Market
  match: Match
  odds: Float!
  prediction: String!
  stake: Float!
  status: BetStatus!
}

input BetInput {
  marketId: String!
  matchId: String!
  odds: Float!
  prediction: String!
  stake: Float!
  status: BetStatus
}

enum BetStatus {
  LOST
  PENDING
  VOID
  WON
}

input CreateLeagueInput {
  country: String!
  logo: String!
  name: String!
}

input CreateTeamInput {
  isFavorite: Boolean
  leagueId: String!
  logo: String!
  name: String!
  streak: [StreakType!]
  tagIds: [String!]
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type League {
  country: String!
  createdAt: DateTime!
  id: ID!
  logo: String!
  name: String!
  teams: [Team!]
}

input LoginInput {
  email: String!
  password: String!
}

type Market {
  bets: [Bet!]
  id: ID!
  name: String!
}

input MarketInput {
  name: String!
}

type Match {
  awayTeam: Team
  bets: [Bet!]
  createdAt: DateTime!
  homeTeam: Team
  id: ID!
  startDate: DateTime!
  status: MatchStatus!
}

input MatchInput {
  awayTeamId: String!
  homeTeamId: String!
  startDate: String!
  status: MatchStatus
}

enum MatchStatus {
  FINISHED
  LIVE
  UPCOMING
}

type MessageResponse {
  message: String!
}

type Mutation {
  createBet(input: BetInput!): Bet!
  createLeague(input: CreateLeagueInput!): League!
  createMarket(input: MarketInput!): Market!
  createMatch(input: MatchInput!): Match!
  createTag(input: TagInput!): Tag!
  createTeam(input: CreateTeamInput!): Team!
  deleteBet(id: String!): Bet!
  deleteLeague(id: String!): League!
  deleteMarket(id: String!): Market!
  deleteMatch(id: String!): Match!
  deleteTag(id: String!): Tag!
  deleteTeam(id: String!): Team!
  executeSeed: String!
  login(input: LoginInput!): AuthResponse!
  logout: MessageResponse!
  refreshTokens(input: RefreshTokenInput!): AuthResponse!
  register(input: RegisterInput!): MessageResponse!
  updateBet(id: String!, input: BetInput!): Bet!
  updateLeague(id: String!, input: UpdateLeagueInput!): League!
  updateMarket(id: String!, input: MarketInput!): Market!
  updateMatch(id: String!, input: MatchInput!): Match!
  updateTag(id: String!, input: TagInput!): Tag!
  updateTeam(id: String!, input: UpdateTeamInput!): Team!
  verifyEmail(input: VerifyEmailInput!): MessageResponse!
}

type Query {
  bet(id: String!): Bet
  bets: [Bet!]!
  league(id: String!): League
  leagues: [League!]!
  market(id: String!): Market
  markets: [Market!]!
  match(id: String!): Match
  matches: [Match!]!
  me: User!
  tag(id: String!): Tag
  tags: [Tag!]!
  team(id: String!): Team
  teams(filter: TeamFilterInput): [Team!]!
}

input RefreshTokenInput {
  refreshToken: String!
}

input RegisterInput {
  email: String!
  password: String!
  username: String
}

enum StreakType {
  DRAW
  LOSE
  WIN
}

type Tag {
  id: ID!
  label: String!
  teams: [Team!]
}

input TagInput {
  label: String!
}

type Team {
  id: ID!
  isFavorite: Boolean
  league: League
  logo: String!
  name: String!
  streak: [StreakType!]
  tags: [Tag!]
}

input TeamFilterInput {
  isFavorite: Boolean
  leagueId: String
  name: String
}

input UpdateLeagueInput {
  country: String
  logo: String
  name: String
}

input UpdateTeamInput {
  isFavorite: Boolean
  leagueId: String
  logo: String
  name: String
  streak: [StreakType!]
  tagIds: [String!]
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  isEmailVerified: Boolean!
  isTwoFactorEnabled: Boolean!
  role: UserRole!
  updatedAt: DateTime!
  username: String
}

enum UserRole {
  ADMIN
  USER
}

input VerifyEmailInput {
  code: String!
  userId: String!
}