'use client';
import React, { useState, useEffect } from 'react';
import type { ReactElement } from 'react';

export const Teams = (): ReactElement => {
  return (
    <div className="w-full h-full flex flex-col gap-5">dwadaw</div>
  );

};